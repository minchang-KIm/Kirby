# Lab 02: 입력 매핑 확장

목표:
- 입력 장치 추가 시 gameplay 코드 변경 없이 대응

과제:
1. 키보드 P2 매핑을 다른 키셋으로 바꾸는 프로필을 추가한다.
2. `build_keyboard_commands()` 테스트를 새 프로필 기준으로 1개 추가한다.
3. 패드 버튼 매핑을 사용자 설정에서 읽도록 확장한다.

완료 체크:
- `input/devices.py`에 하드코딩 버튼 인덱스 직접 참조가 남지 않음
