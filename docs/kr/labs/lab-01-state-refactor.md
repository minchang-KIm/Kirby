# Lab 01: 상태 전이 리팩터링

목표:
- 상태 전이 규칙을 `if` 체인에서 전이 테이블 기반으로 이동

과제:
1. `ActorState`에 `Fall` 진입 조건을 한 곳으로 모은다.
2. `transition()`을 통하지 않는 상태 변경 코드를 찾아 제거한다.
3. 불법 전이 테스트를 추가한다.

완료 체크:
- `test_state_machine_new.py`에 불법 전이 테스트가 1개 이상 추가됨
